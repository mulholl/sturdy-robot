#ifndef FILE_EXCEPTIONS_HPP
#define FILE_EXCEPTIONS_HPP

#include <exception>

// struct OpenFailed { }; // Failed to open a file
// struct test_exc { };

namespace fexc {
	struct OpenFailed { };
}

#endif